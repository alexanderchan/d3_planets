// require('dotenv').config({silent: true}) // load config from .env or environment
// const assert = require('chai').assert
// // can use supertest to test express
// describe('index', () => {
//   it('should do something', () => {})
// })
